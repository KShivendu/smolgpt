/target
model.bin
*.bin
data/arithmetic*.txt
# Cached eval results written by the --serve web UI; regenerable via /api/models/{id}/eval.
*.eval.json
# SQLite-backed model registry (regenerable from --train runs + models.toml seed).
smolgpt.db
smolgpt.db-wal
smolgpt.db-shm
*.bak
# Jacobian-lens analysis output (PNG plots written by analysis/jacobian_lens.py,
# cached/served via the jacobian_lens_results DB table); regenerable via
# --train --jacobian-lens or the --serve Jacobian/Embeddings tabs.
jacobian_lens_output/
# Python analysis: bytecode cache and regenerable plot/JSON output.
__pycache__/
*.pyc
analysis/output/
# Python port: torch checkpoints and uv virtualenv.
*.pt
python/.venv/
.pytest_cache/
